import { SalesPerson } from '@/lib/types';

// SALES_TEAM is now derived from the profiles table via listSalesTeam().
// This file kept as a type-safe fallback shape for legacy imports.
export const SALES_TEAM: SalesPerson[] = [];
